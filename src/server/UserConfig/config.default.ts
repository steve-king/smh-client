import { UserConfig } from '@/types'

const config: UserConfig = {
  nodes: [],
  services: [],
  settings: {
    cronInterval: 1,
    email: '',
    coingeckoApiKey: '',
  },
}

export default config
